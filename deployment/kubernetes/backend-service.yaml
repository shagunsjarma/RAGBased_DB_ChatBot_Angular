apiVersion: v1
kind: Service
metadata:
  name: rag-chatbot-backend-service
  namespace: default
spec:
  selector:
    app: rag-chatbot-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
